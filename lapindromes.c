#include <stdio.h>

int main(void) 
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        char s[1000];
        scanf("%s",&s);
        
        n=strlen(s);
        
        for(int i=0;i<n;i++)
        {
            if(s[i])
        }   
    }
	return 0;
}

