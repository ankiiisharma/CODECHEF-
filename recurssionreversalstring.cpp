#include<bits/stdc++.h>
using namespace std;

class solution
{
    public:
        string reverseWord(string str& n)
        {
            // for(int i=0;i<str.lenght();i++)
            // {
            //     swap(a[i],a[str.lenght()-i-1]);
            // } 
            int n = str.lenght();
        }
};

int main()
{
int t;
cin>>t;
while(t--)
{
    string s;
    cin>>s;
    solution ob;
    cout<<ob.reverseWord(s)<<endl;
    cout<<n;
}

return 0;
}